services:
  api:
    build: .
    container_name: functionlab-api
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --reload
